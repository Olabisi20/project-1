
package model;

import java.awt.Color;

public class BlueMissile implements MissileImplementator{

    @Override
    public void launch(float sx, float sy, float tx, float ty, Color color) {
            
           // double distance = target.distance(super.x, super.y);
           // boolean targetReached = distance <= 2.0;
    }
    
}
